# 変更の記録

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に倣います。

## 版のつけ方

`メジャー.マイナー.パッチ` の 3 つ組で、**パッチは 3 桁のゼロ詰め**です。
修正や追加をするたび **+0.0.001** ずつ進めます。

```
0.9.000 → 0.9.001 → 0.9.002 → … → 0.9.999 → 0.10.000
```

進めるときは手で書き換えず、次のコマンドを使います。
`koyomi/__init__.py` の `APP_VERSION` と `pyproject.toml` の `version` を
まとめて書き換えます。

```bash
python tools/bump_version.py
```

```bash
python tools/bump_version.py --show
```

---

## 未リリース

### 追加予定

- メニューからの更新確認（GitHub へ公開したあとに着手）

---

## [0.9.001] - 2026-08-29

### 直した

- 連動動作で画像・文書などの実行ファイル以外を指定すると開けなかった
  （`WinError 193`）。実行ファイルはこれまでどおり直接起動し、
  それ以外は OS の関連付け（ダブルクリックと同じ経路）で開くようにした
- 連動動作の欄の呼び名を「アプリ」から「開くもの」に変更。
  引数は実行ファイルのときだけ使われることを明記

---

## [0.9.000] - 2026-08-28

最初の公開版。

### アラーム

- 繰り返し 8 種（繰り返さない／日付指定／曜日／N 日おき／毎月の日付（月末可）／
  毎月の第 n 曜日（最終週可）／毎年／鳴動・休止の周期）
- 鳴らさない日 — 日本の祝日（jpholiday）、8 本の日付リスト、次の 1 回だけスキップ
- 止め方 6 種（押す／長押し／スライド／計算問題／順番タップ／色と形）。
  停止用とスヌーズ用を別々に設定でき、難易度と出題数も指定可。
  選んだ操作方法に関係のない欄は見出しごと隠れる
- スヌーズ — 間隔、最大回数（無制限可）、間隔の起点、鳴動中の間隔変更、
  1 回目 / 2 回目 / 3 回目以降で別の音
- 音 — 内蔵音 6 種（起動時に合成）、音声ファイル、フォルダからランダム、無音。
  音量、フェードイン、鳴り始めの 2 秒遅延
- 自動停止。スヌーズ間隔をまたぐ場合は次回の 5 秒前で切り上げ

### 一覧と整理

- グループ 5 種（名前変更可）、名前・グループ名での検索、5 通りの並び替え
- ON/OFF スイッチの固定、複製、まとめて設定、指定時刻までを止める
- OFF のアラームの一括削除

### PC まわり

- スリープ解除タイマーの自動予約（Windows）
- 鳴動中のスリープ抑止、指定時刻の自動スリープ
- 電源設定の確認（管理者権限なしで判定）
- アラーム連動でのアプリ起動・Web ページ表示。
  外部から復元した連動動作は承認するまで実行しない

### タイマー

- カウントダウン最大 10 本。終了日時を保持するのでアプリを閉じても継続
- 期間指定と日時指定の 2 通り。秒単位から年単位まで
- ストップウォッチ（1 台 1 窓・台数無制限・最前面固定・秒の精度 4 段階）

### そのほか

- 世界時計（主要 23 都市＋全 IANA タイムゾーン検索）
- やることリスト（優先度 3 段階・期限・メモ）
- 外観テーマ 27 種（下地 3 系統 × さし色 9 色）
- フローティング表示（枠なし・最前面・自由移動）
- 日本語／英語の切り替え
- タスクトレイ常駐、取りこぼしアラームの検出、JSON でのバックアップと復元
