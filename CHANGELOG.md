# 変更の記録

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に倣います。

## 版のつけ方

`メジャー.マイナー.パッチ` の 3 つ組で、**パッチは 3 桁のゼロ詰め**です。
修正や追加をするたび **+0.0.001** ずつ進めます。

```
0.9.000 → 0.9.001 → 0.9.002 → … → 0.9.999 → 0.10.000
```

進めるときは手で書き換えず、次のコマンドを使います。
`koyomi/__init__.py` の `APP_VERSION` と `pyproject.toml` の `version` を
まとめて書き換えます。

```bash
python tools/bump_version.py
```

```bash
python tools/bump_version.py --show
```

---

## 未リリース

---

## [0.9.018] - 2026-09-22

### 直した

- **英語版など日本語以外の Windows では、ショートカットを作る道具（tools/make_shortcut.py）が失敗していた。**
  使っていた WScript.Shell が文字をいったん OS の文字コードに直すため、
  「こよみアラーム.lnk」という名前が「?」に化けて保存できなかった。
  Windows のショートカットの部品（IShellLinkW）に UTF-16 のまま渡すようにした。
  PowerShell を立ち上げなくなったので、作るのも速い
- GitHub の CI は英語版の Windows で走るため、0.9.004 から毎回このテストで失敗していた。
  テストが通ると次に止まるはずだった対訳表の検査（日本語を書き出すところで落ちる）も、
  CI の出力を UTF-8 にして防いだ

### そのほか

- テストを走らせる道具 tools/run_tests.py を足した。終わりに、時間のかかったテストの上位を出す。
  CI ではこれを使い、落ちたテストと時間のかかったテストを注記として実行結果の画面に出す
  （CI のログはサインインしないと読めない）
- テスト一式が手元で約 90 秒かかっていたのを、約 4 秒にした。
  テストの準備で同じスタイルシートを毎回当て直していて、それまでに作った窓が
  すべて塗り直され、1 件ごとに遅くなっていた。CI で 10 分ほどかかっていたのもこのため

---

## [0.9.017] - 2026-09-22

### 直した

- **英語版など日本語以外の Windows では、起動すると本体の窓を作るところで落ちていた。**
  見出しに今日の日付（「2026年09月22日」）を書くとき、Windows の strftime が
  「年」「月」「日」を扱えなかった。日本語版の Windows と、Python 3.12 では起きない。
  GitHub の CI は英語版の Windows で走るため、Python 3.10 のほうは 0.9.000 から毎回ここで失敗していた
- 英語版 Windows の文字コードに切り替えて確かめるテストを足した。
  日本語版の手元でも、同じ落ち方をテストで見つけられる

### そのほか

- CI で使う actions/checkout と actions/setup-python を v7 にした。
  Node.js 20 の廃止予告の警告が出なくなる

---

## [0.9.016] - 2026-09-17

### 増やした

- **「毎月・第n曜日」で、週を複数選べる**ようにした。
  「第2・第4木曜」のような指定が、アラーム 1 件でできる。「最終」とも組み合わせられる
- 一覧には「毎月 第2・第4木曜」のように、選んだ週がすべて出る
- 前の版で作ったアラームは、選んでいた週 1 つのまま鳴る
- 英語表示では、週を「2nd, 4th Thu」のように序数で出す（画面の幅に収まるように）

---

## [0.9.015] - 2026-09-15

### 直した

- **テストを走らせると、本物の保存先に見本のアラームを書き込んでいた。**
  説明どおり `python -m unittest discover -s tests` で走らせると
  tests/__init__.py が読まれず、%APPDATA% の差し替えが効いていなかった。
  各テストの先頭で置き場を差し替え、先に本物を向いていたら止めるようにした

---

## [0.9.014] - 2026-09-15

### 変えた

- **曜日の並びを日曜はじめにした。**「月火水木金土日」を「日月火水木金土」に
  - アラームの編集の曜日指定と、毎月・第n曜日の曜日
  - 日付リストの管理の「1年分を追加」の曜日
  - 一覧の繰り返しの表示（「木・日」→「日・木」）
- 日付を選ぶカレンダーも、OS の地域設定によらず日曜はじめにそろえた
- 保存している曜日の番号は変えていない。いまのアラームはそのまま鳴る

---

## [0.9.013] - 2026-09-15

### 増やした

- **窓の大きさを覚える**ようにした。トレイやタスクバーから開き直したときも、
  次に起動したときも、閉じたときの大きさで開く
  - 本体・タイマー・世界時計・やることリストは、位置と最大化も戻す
  - アラームの編集・設定・まとめて設定・日付リストの管理は、大きさだけを戻す。
    位置はこれまでどおり本体の真ん中に出る
  - 画面が小さくなっていたら、はみ出さないように縮める
  - 自動起動でトレイに畳んだまま一度も開かなかったときは、覚えていた大きさを残す
- トレイへ畳むたびに設定を保存する。畳んだあとに Windows ごと終了しても、
  窓の大きさが残るように

### 直した

- 最大化していた本体をトレイから開くと、最大化が解けていた

---

## [0.9.012] - 2026-09-13

### 変えた

- **カウントダウンは、足したその場で計り始める**ようにした。
  「すぐ足す」のボタンも「追加」も同じ。日時を指定したタイマーは、
  足すまでに経った分も含めて、その時刻ちょうどに終わる

### 増やした

- **カウントダウンの終わりの音を選べる**ようにした。内蔵の音のほか、
  音声ファイル、フォルダからランダム、音なしから選べる。音量と、
  だんだん大きくする指定もできる
- 音はタイマーごとに覚える。行には「♪ 音の名前」を添える
- 音の選択欄は、ふだん音の名前 1 行に畳んでおき、「音を選ぶ」で開く。
  開いたままだと、タイマーの一覧が 1 本半ほどしか見えなくなるため
- 最後に選んだ音は、次にタイマーの窓を開いたときも選ばれている
- 音声ファイルが見つからないときは内蔵の音で鳴らし、そのことを知らせる

### 移行

- これまでに足してあったタイマーは、以前と同じ「ひびき」で鳴る

---

## [0.9.011] - 2026-09-13

### 直した

- **「次の 1 回だけ飛ばす」が、飛ばした回を過ぎても外れたように見えなかった。**
  見張りは時刻に合わせて指定を下ろしていたが、一覧の札を描き直さず、
  保存もしていなかった。札は「次は飛ばす」のまま次の日時の横に残るので、
  続く回まで飛ばすように見えていた
- 指定を下ろしたことを保存する前にアプリが落ちると、指定が戻っていた。
  木・日のアラームなら、日曜を飛ばしたあと木曜まで飛ばしていた
- 飛ばす回の時刻にアプリが動いていなかったときも、続く回を代わりに飛ばしていた
- 飛ばす回の直後に起動し直すと、その回を「鳴らせなかったアラーム」として知らせていた
- 鳴ったアラームを止めると、後の回に付けてあった「次は飛ばす」まで外れていた

### 変えた

- 飛ばす回を **日付で覚える** ようにした（`skip_on`）。その回の時刻を過ぎたら、
  アプリが動いていたかどうかに関わらず外れ、そのことを保存する
- 一覧の札に日付を出す。「9/13(日) は飛ばす」
- 編集画面で保存したときは、同じ回がまだ予定にあればそれを引き継ぐ。
  時刻や曜日を変えてその回が無くなったら、新しい予定の次の回を飛ばす

### 移行

- 0.9.010 までに付けた「次は飛ばす」は日付を持たない。読み込んだときに、
  最後に鳴ったあとの最初の回を指すものとして日付を補う。
  鳴った記録が無ければ、前回アプリが動いていた時刻から数える

---

## [0.9.010] - 2026-09-12

### 増やした

- **連動動作に「いま試す」を付けた。**
  編集画面の「連動」タブから、アラームの時刻を待たずにその場で動かせる。
  真夜中に動かす指定は、これまで確かめようがなかった
- 試した結果には、**渡した引数を 1 つずつ並べて見せる**。
  引数の区切りが意図どおりに分かれているかは、ここで目に見える
- 同じ場所に「実行の記録」を置いた。`actions.log` をその場で開く
- 画面を出さない設定のアラームで「この操作を試す」を押したときの案内を、
  「いま試す」へ案内する文に改めた

### 直した

- README と CHANGELOG に紛れ込んでいた制御文字を取り除いた。
  `%APPDATA%\Koyomi\actions.log` が `%APPDATA%\Koyomictions.log` と
  化けていた

### 覚え書き

- 「1 回だけ」のアラームは、指定した時刻が今日すでに過ぎていれば翌日に鳴る。
  取りこぼしを拾うのは、アプリが動き出したときから
  `catch_up_window_minutes`（既定 60 分）以内に過ぎた分だけ

---

## [0.9.009] - 2026-09-11

### 直した

- **連動動作の引数から、逆斜線が消えてしまう不具合。**
  `shlex.split` は逆斜線を「次の 1 文字を逃がす記号」として扱うため、

  ```
  D:\XAMPP\htdocs\DMM\stock_init.php   ->   D:XAMPPhtdocsDMMstock_init.php
  ```

  のように区切りが落ちていた。渡された側は当然ファイルを開けず、
  それでいて「起動しました」とだけ出るので、気付きにくかった。
  引用符の扱いはそのままに、Windows では逃がし記号を取り止めた

### 増やした

- 画面を出さずに実行したときの **出しものを `%APPDATA%\Koyomi\actions.log`
  に残す**ようにした。動かしたものが画面へ出すはずだった内容が入る。
  200 KB を超えたら書き直す
- そのときは黒い窓も出さない（`CREATE_NO_WINDOW`）。
  ふつうに鳴らすアラームから動かす場合は、これまでどおり窓が出る
- 引数に書いたファイルが見つからないときは、保存する前に知らせる。
  絶対パスに見えるものだけを見るので、`--station` のような指定は素通りする

### 注意

- 作業フォルダは「開くもの」に指定したプログラムの置き場所。
  スクリプトが相対パスを使う場合は、絶対パスで書くのが確実

---

## [0.9.008] - 2026-09-11

### 変えた

- 拡張子 `.pyw` の関連付けが別の Python に取られていても、
  **ダブルクリックでそのまま開くようにした。**
  部品の入っている Python をこのパソコンの中から探して、そちらへ渡し直す。
  探す順は `PATH` → レジストリの登録 → よくある置き場所。
  動かして確かめると 1 つにつき 1 秒近くかかるので、
  site-packages に居るかどうかだけを見る
- 渡し直しは一度きり。渡した先でも足りなければ、そこで知らせて終わる
- 渡し直せたときは控えに残さない。
  「`error.log` がある＝つまずいた」の目印を濁らせないため。
  コンソールを付けて開いた場合だけ、どこへ渡したかを一行出す
- 見つからなかったときの文面を、いまの筋道に合わせた

### 注意

- 渡し直す分だけ起動が少し遅くなる。
  気になる場合は `.pyw` の関連付けを直すのが確実
  （右クリック → プログラムから開く → 別のプログラムを選択 → `pythonw.exe`）

---

## [0.9.007] - 2026-09-10

### 増やした

- 繰り返しに **「毎日」** を追加。8 種類から 9 種類になった。
  「曜日を指定」で 7 つとも選んでも同じ日に鳴るが、
  やりたいことをそのまま選べるようにした。決める項目は無い

### 変えた

- 一覧の表示が「毎週 毎日」ではなく **「毎日」** になる。
  曜日を 7 つ選んだ場合とは、見た目で区別が付く

### そのまま

- 「祝日を除く」「日付リストを除く」「次の 1 回だけ飛ばす」は今までどおり重ねられる
- 保存の形は変わらない。古い設定はそのまま読める

---

## [0.9.006] - 2026-09-09

### 増やした

- **`install.bat`**。ダブルクリックで、使う Python を選び、
  `pip install -r requirements.txt` を済ませ、
  入ったかどうかを確かめて、デスクトップにショートカットまで作る。
  `python` が無ければ `py -3` を試し、どちらも無ければ入れ方を案内する。
  ショートカットが要らないときは `install.bat /noshortcut`

### 変えた

- **起動口を `run.pyw` ひとつにした。** `run.py` は削除。
  2 つ並んでいると、初めて使う人がどちらで開くのか迷うため
- 出力の付け替えは、出力先が無いとき（`pythonw` で開いたとき）だけ行う。
  `python run.pyw` で開けば、これまでの `run.py` と同じく
  コンソールにそのまま出る
- 部品が足りないときの知らせも、コンソールがあればそちらへ出す

### 注意

- 自動起動の登録先が `run.py` から `run.pyw` に変わる。
  古い登録が残っている場合は、次に開いたときに登録し直すか尋ねる

---

## [0.9.005] - 2026-09-02

### 増やした

- **画面を出さずに実行するアラーム**。時刻が来たら鳴動画面も音も出さず、
  連動して起動するものだけを動かす。夜間のバックアップのように、
  見せる必要のない仕事に向く
- **実行したことを通知で知らせる**かどうかの選択。既定は入

### 変えた

- 画面を出さない設定にすると、関わりのない欄が引っ込む。
  音のタブ、止め方の操作、鳴り続ける時間、スヌーズ、鳴動画面、
  連動の「いつ」の指定。値そのものは残るので、外せば元どおりになる
- 「止めたら削除」と「スイッチの固定」は画面と関わりが無いので残す
- 一覧に「画面なし」の目印を出す

### 注意

- 止める操作が無いので、スヌーズも自動停止もしない
- 連動の「鳴り始めたとき／止めたとき」は使わず、時刻が来た時点で実行する
- 試し鳴動では通知の出かたを見せるだけで、連動動作は動かさない
- ほかのアラームと重なっても、画面を取らないので譲らない

---

## [0.9.004] - 2026-09-02

### 増やした

- **コンソールを出さない起動口** `run.pyw`
- **ショートカットを作る道具** `tools/make_shortcut.py`。
  いま使っている Python を名指しした `.lnk` をデスクトップに置く。
  `--start-menu` `--minimized` `--where` を付けられる
- ショートカット用のアイコン（`.ico`）も、これまでの絵からその場で組み立てる
- つまずいたときの記録を `%APPDATA%\Koyomi\error.log` に残すようにした。
  `pythonw.exe` には出力先が無く、そのままでは何も残さずに消えてしまうため。
  開始できなかったときは、その場でお知らせも出す

### 変えた

- 部品が出す注意書き（「そのうち無くなります」の類）は記録に混ぜない。
  ファイルがあること自体を「つまずいた」の目印にするため
- 記録は書くときだけ開いて閉じる。動かしている間もファイルを消したり移したりできる
- 記録が 200 KB を超えたら、古い分を捨てて書き直す

### そのまま

- コンソール付きで始めたいときは `python run.py`（0.9.006 で `run.pyw` に一本化）
- 拡張子 `.pyw` の関連付けが別の Python に取られていると、
  直接ダブルクリックしても開けない。ショートカットならその心配が無い。
  そうなった場合は、どの Python で開こうとしたかを添えて知らせる
- 自動起動の登録は前から `pythonw.exe` を名指ししているので、
  今回の変更で登録し直す必要はない

---

## [0.9.003] - 2026-08-29

### 増やした

- **Windows の起動時に始める**設定。現在のユーザーの Run キーへ登録するので
  管理者権限は要らない。既定は切
- 自動起動のときは **トレイに畳んだ状態で立ち上がる**（`--minimized`）。既定は入
- 登録の状態を設定画面に表示。「別の場所を指している」
  「タスクマネージャーで無効にされている」も見分ける。
  フォルダを移動した場合は起動時に知らせて登録し直しを促す
- **二重起動の防止**。あとから起動したものは、すでに動いている窓を前に出して
  自分は終わる。同じアラームが二度鳴るのを防ぐため
- **更新の確認と取り込み**。メニューの「更新を確認」から、
  新しい版があればその場で `git pull --ff-only` で取り込み、開き直せる。
  起動時に自動で調べる設定も追加（既定は切）

### 注意

- 自動起動はフォルダを移動すると外れる。設定画面の「登録し直す」で直せる
- 更新の取り込みは git の作業コピーのときだけ。手元に未保存の変更があると止まる

---

## [0.9.002] - 2026-08-29

### 増やした

- 鳴らす時刻を **秒まで** 指定できるようにした。
  一覧では秒を設定したときだけ `06:30:15` の形で見せ、
  秒が 0 のときはこれまでどおり `06:30` と出す
- 時刻の入力欄で、**カーソルを合わせた桁がそのままホイールで動く**ようにした。
  以前は一度クリックして選んだ桁しか変えられなかった。
  時・分・秒のどこでも、合わせて回すだけで変わる

### 変えた

- 発火の見張りを 1 秒ごとから 0.25 秒ごとにした。
  秒を指定したアラームの遅れが 1 秒から 0.25 秒以内に縮む。
  一覧の描き直しはこれまでどおり 1 秒に 1 回
- 閉じるボタンで畳んだときの「常駐しています」の案内を、
  **最初の 1 回だけ**に変更。表示したことは保存され、
  アプリを終了しても二度目は出ない

保存済みのアラームはそのまま読めます（秒は 0 として扱われます）。

---

## [0.9.001] - 2026-08-29

### 直した

- 連動動作で画像・文書などの実行ファイル以外を指定すると開けなかった
  （`WinError 193`）。実行ファイルはこれまでどおり直接起動し、
  それ以外は OS の関連付け（ダブルクリックと同じ経路）で開くようにした
- 連動動作の欄の呼び名を「アプリ」から「開くもの」に変更。
  引数は実行ファイルのときだけ使われることを明記

---

## [0.9.000] - 2026-08-28

最初の公開版。

### アラーム

- 繰り返し 8 種（繰り返さない／日付指定／曜日／N 日おき／毎月の日付（月末可）／
  毎月の第 n 曜日（最終週可）／毎年／鳴動・休止の周期）
- 鳴らさない日 — 日本の祝日（jpholiday）、8 本の日付リスト、次の 1 回だけスキップ
- 止め方 6 種（押す／長押し／スライド／計算問題／順番タップ／色と形）。
  停止用とスヌーズ用を別々に設定でき、難易度と出題数も指定可。
  選んだ操作方法に関係のない欄は見出しごと隠れる
- スヌーズ — 間隔、最大回数（無制限可）、間隔の起点、鳴動中の間隔変更、
  1 回目 / 2 回目 / 3 回目以降で別の音
- 音 — 内蔵音 6 種（起動時に合成）、音声ファイル、フォルダからランダム、無音。
  音量、フェードイン、鳴り始めの 2 秒遅延
- 自動停止。スヌーズ間隔をまたぐ場合は次回の 5 秒前で切り上げ

### 一覧と整理

- グループ 5 種（名前変更可）、名前・グループ名での検索、5 通りの並び替え
- ON/OFF スイッチの固定、複製、まとめて設定、指定時刻までを止める
- OFF のアラームの一括削除

### PC まわり

- スリープ解除タイマーの自動予約（Windows）
- 鳴動中のスリープ抑止、指定時刻の自動スリープ
- 電源設定の確認（管理者権限なしで判定）
- アラーム連動でのアプリ起動・Web ページ表示。
  外部から復元した連動動作は承認するまで実行しない

### タイマー

- カウントダウン最大 10 本。終了日時を保持するのでアプリを閉じても継続
- 期間指定と日時指定の 2 通り。秒単位から年単位まで
- ストップウォッチ（1 台 1 窓・台数無制限・最前面固定・秒の精度 4 段階）

### そのほか

- 世界時計（主要 23 都市＋全 IANA タイムゾーン検索）
- やることリスト（優先度 3 段階・期限・メモ）
- 外観テーマ 27 種（下地 3 系統 × さし色 9 色）
- フローティング表示（枠なし・最前面・自由移動）
- 日本語／英語の切り替え
- タスクトレイ常駐、取りこぼしアラームの検出、JSON でのバックアップと復元
